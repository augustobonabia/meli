describe('buildSearchResponseItems', () => {
  it.todo('la función buildSearchResponseItemPrice debe construir el precio de un item con el formato definido para la API', () => {

  });

  it.todo('la función buildSearchResponseItems deve devolver una lista con items en el formato definido para la API', () => {

  });
});

describe('buildSearchResponseCategories', () => {
  it.todo('la función buildSearchResponseCategories tiene que devolver un array de nombres decategorías cuando hay un filtro aplicado', () => {});

  it.todo('la función buildSearchResponseCategories tiene que devolver un array de nombres de categorías cuando no hay un filtro aplicado'
        + 'y hay filtros de categorías para aplicar', () => {
  });
});
